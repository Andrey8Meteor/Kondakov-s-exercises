// Внешняя функция, которую мы будем мокать в тестах
export const saveOrder = async (orderId) => {
  // Имитация реальной работы с базой данных
  // В реальном приложении здесь был бы запрос к БД
  return Math.random() > 0.5; // случайный результат для демонстрации
};
